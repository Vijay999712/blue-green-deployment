apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
    version: green  # 🔁 Change this to blue or green
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
